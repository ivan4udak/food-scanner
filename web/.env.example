# Backend для dev-прокси Vite (npm run dev). В проде проксирует nginx (см. nginx.conf).
VITE_API_TARGET=http://localhost:8080

# Базовый путь API. Фронт всегда ходит на относительный /api/v1 (один origin).
# Менять не нужно — оставлено для гибкости.
VITE_API_BASE=/api/v1
