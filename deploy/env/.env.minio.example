# ОБЩИЙ MinIO — на сервере: /opt/foodscanner/minio/app.env (chmod 600).
# Эти же значения должны совпадать с MINIO_ACCESS_KEY / MINIO_SECRET_KEY в app.env
# каждого окружения (staging/production).

MINIO_ROOT_USER=foodscanner
MINIO_ROOT_PASSWORD=CHANGE_ME_shared_minio_password
